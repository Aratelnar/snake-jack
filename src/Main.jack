class Main {
    function void main(){
        var Game game;
        do FieldDrawer.init(512,256,15); // screen dimensions + cell size
        let game = Game.new(Field.new(15, 15));
        do game.run();
        do game.dispose();
        return;
    }
}
